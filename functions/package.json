{
  "name": "planterior-functions-contracts",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test lib/**/*.test.js",
    "test:emulator": "export METADATA_SERVER_DETECTION=none && npm run build && node --test lib/**/*.emulator-spec.js"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.2",
    "jose": "6.1.3",
    "ky": "1.10.0"
  },
  "overrides": {
    "uuid": "11.1.1"
  },
  "devDependencies": {
    "@types/express": "5.0.6",
    "@types/node": "26.2.0",
    "express": "5.2.1",
    "typescript": "7.0.2"
  }
}
